# 🎭 AI Meme Generator

Генератор мемов с искусственным интеллектом. Загрузи фото → AI придумает текст → Стань легендой!

## ✨ Возможности

- 🤖 **AI-генерация** — автоматическая генерация текста для мемов через Pollinations API
- 🔐 **Авторизация** — регистрация через email/пароль, Google, GitHub
- 📸 **Личная галерея** — сохранение созданных мемов в личном кабинете
- 🌍 **Публичная лента** — просмотр мемов всех пользователей
- 🔒 **Контроль видимости** — выбор между публичным и приватным доступом
- 🛡️ **Безопасность** — валидация данных, rate limiting, защита от brute-force
- 📱 **Адаптивный дизайн** — работает на десктопе и мобильных устройствах

## 🚀 Быстрый старт

### Требования

- Node.js 18+
- npm или yarn

### Установка

```bash
# Клонирование репозитория
git clone https://github.com/mlyaho/ai-mem-generator.git
cd ai-mem-generator

# Установка зависимостей
npm install

# Запуск разработки
npm run dev
```

Откройте [http://localhost:3000](http://localhost:3000) в браузере.

## 📁 Структура проекта

```
src/
├── app/
│   ├── api/
│   │   ├── auth/
│   │   │   ├── [...nextauth]/route.ts  # NextAuth + brute-force защита
│   │   │   └── register/route.ts       # Регистрация + валидация + rate limit
│   │   └── memes/
│   │       ├── route.ts                # CRUD мемов + валидация URL + rate limit
│   │       └── [id]/visibility/route.ts # Изменение видимости
│   ├── auth/
│   │   ├── signin/page.tsx             # Страница входа
│   │   └── signup/page.tsx             # Страница регистрации
│   ├── feed/              # Лента публичных мемов
│   ├── profile/           # Личный кабинет пользователя
│   ├── layout.tsx         # Основной layout + env validation
│   └── page.tsx           # Главная (генератор мемов)
├── components/
│   ├── AIPrompt.tsx       # Форма ввода промпта
│   ├── ImageUpload.tsx    # Загрузка изображения
│   ├── MemeGallery.tsx    # Галерея мемов
│   ├── MemePreview.tsx    # Предпросмотр мема
│   └── Providers.tsx      # SessionProvider
├── lib/
│   ├── prisma.ts          # Prisma клиент
│   ├── validators.ts      # Zod схемы валидации
│   ├── rateLimit.ts       # Rate limiting middleware
│   └── env.ts             # Проверка переменных окружения
├── middleware.ts          # CSP заголовки + security headers
└── services/
    ├── ApiFactory.ts      # Фабрика API сервисов
    ├── PollinationsService.ts
    ├── YandexGPTService.ts
    ├── KandinskyService.ts
    └── GigaChatService.ts
```

## 🛠 Стек технологий

| Категория | Технологии |
|-----------|------------|
| Framework | Next.js 16 (App Router) |
| Язык | TypeScript |
| Стили | Tailwind CSS v4 |
| База данных | SQLite (dev) / PostgreSQL (prod) |
| ORM | Prisma 5 |
| Авторизация | NextAuth.js v5 (Auth.js) |
| Валидация | Zod |
| Rate Limiting | express-rate-limit |
| AI API | Pollinations, YandexGPT, Kandinsky, GigaChat |

## 🔐 Безопасность

### Реализованные меры защиты

| Угроза | Защита |
|--------|--------|
| **Brute-force атаки** | Блокировка после 5 неудачных попыток (15 мин) |
| **Rate limiting** | 5/мин (auth), 30/мин (api), 10/мин (ai) |
| **XSS атаки** | CSP заголовки, валидация URL изображений |
| **SQL-инъекции** | Prisma ORM с параметризованными запросами |
| **SSRF атаки** | Блокировка внутренних IP в URL |
| **Слабые пароли** | Требование: 8+ символов, заглавные+строчные+цифры |
| **Уязвимости сессий** | Secure cookies, httpOnly, sameSite: lax |

📄 Подробнее в [SECURITY.md](./SECURITY.md)

## 🔐 Настройка OAuth (опционально)

Для входа через Google/GitHub добавьте переменные в `.env`:

### Google OAuth
1. Откройте [Google Cloud Console](https://console.cloud.google.com/)
2. Создайте проект → Credentials → OAuth 2.0 Client ID
3. Redirect URI: `http://localhost:3000/api/auth/callback/google`

```env
GOOGLE_CLIENT_ID=your-client-id
GOOGLE_CLIENT_SECRET=your-client-secret
```

### GitHub OAuth
1. Откройте [GitHub Settings → Developer settings](https://github.com/settings/developers)
2. OAuth Apps → New OAuth App
3. Authorization callback URL: `http://localhost:3000/api/auth/callback/github`

```env
GITHUB_CLIENT_ID=your-client-id
GITHUB_CLIENT_SECRET=your-client-secret
```

## 📊 Схема базы данных

```prisma
User {
  id            String   @id @default(cuid())
  email         String   @unique
  passwordHash  String?
  name          String?
  image         String?
  createdAt     DateTime @default(now())
  updatedAt     DateTime @updatedAt
  memes         Meme[]
  accounts      Account[]
  sessions      Session[]
}

Meme {
  id          String   @id @default(cuid())
  userId      String
  user        User     @relation(fields: [userId], references: [id], onDelete: Cascade)
  imageUrl    String   # Валидируется: http/https, не internal IP
  topText     String   @default("")  # Макс. 200 символов
  bottomText  String   @default("")  # Макс. 200 символов
  isPublic    Boolean  @default(true)
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt

  @@index([userId])
  @@index([isPublic])
}

Account { ... }  # NextAuth OAuth аккаунты
Session { ... }  # NextAuth сессии
```

## 🌐 API Endpoints

### Авторизация
| Метод | Endpoint | Защита | Описание |
|-------|----------|--------|----------|
| POST | `/api/auth/register` | Rate limit (5/мин) + Zod валидация | Регистрация нового пользователя |
| GET/POST | `/api/auth/[...nextauth]` | Brute-force защита | NextAuth handler |

### Мемы
| Метод | Endpoint | Защита | Описание |
|-------|----------|--------|----------|
| GET | `/api/memes` | — | Получение мемов (с фильтрами userId, isPublic, cursor) |
| POST | `/api/memes` | Auth + Rate limit + Zod валидация URL | Создание мема (требуется авторизация) |
| DELETE | `/api/memes?id=...` | Auth + Rate limit | Удаление мема (только владелец) |
| PATCH | `/api/memes/[id]/visibility` | Auth + Rate limit | Изменение видимости (только владелец) |

### AI сервисы
| Метод | Endpoint | Защита | Описание |
|-------|----------|--------|----------|
| POST | `/api/pollinations` | Rate limit (10/мин) | Генерация изображений |
| POST | `/api/yandex` | Rate limit (10/мин) | YandexGPT |
| POST | `/api/kandinsky` | Rate limit (10/мин) | Kandinsky |
| POST | `/api/gigachat` | Rate limit (10/мин) | GigaChat |

## 📝 Использование

### Регистрация
1. Перейдите на `/auth/signup`
2. Введите email и пароль:
   - Минимум 8 символов
   - Заглавные и строчные буквы
   - Минимум одна цифра
3. После регистрации вы будете автоматически авторизованы

### Создание мема
1. На главной странице загрузите изображение или сгенерируйте через AI
2. Введите промпт для генерации текста
3. Выберите видимость (🌍 Публичный / 🔒 Приватный)
4. Нажмите "Сохранить в галерею"

### Личный кабинет
- `/profile` — просмотр ваших мемов
- Фильтрация: Все / Публичные / Приватные
- Изменение видимости и удаление мемов

### Лента мемов
- `/feed` — публичные мемы всех пользователей
- Пагинация через кнопку "Загрузить ещё"

## 🚀 Продакшн

### Переключение на PostgreSQL

1. Установите PostgreSQL и создайте базу данных
2. Обновите `.env`:
```env
DATABASE_URL="postgresql://user:password@localhost:5432/meme_db"
NEXTAUTH_URL="https://your-domain.com"
NEXTAUTH_SECRET="your-production-secret"
```
3. Примените миграции:
```bash
npx prisma migrate deploy
```

### Генерация NEXTAUTH_SECRET
```bash
node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
```

### Требования для production

- ✅ NEXTAUTH_SECRET (минимум 32 символа)
- ✅ HTTPS соединение
- ✅ PostgreSQL вместо SQLite
- ✅ OAuth ключи в секретах хостинга
- ✅ Rate limiting настроен

## 📄 Документы

- [README.md](./README.md) — Основная документация
- [SECURITY.md](./SECURITY.md) — Политика безопасности
- [.env.example](./.env.example) — Шаблон переменных окружения

## 📝 Лицензия

MIT

## 👥 Авторы

- [@mlyaho](https://github.com/mlyaho)
